<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 네임스페이스(사용영역) 설정 -->

<mapper namespace="com.classs.skhuter.mappers.BoardMapper">
	<!-- 글쓰기 -->
	<insert id="create">
		INSERT INTO Board (board_no, user_no, title, regdate,
		hit_count, password, content, like_count, hate_count)
		VALUES
		(seq_board.nextVal, #{userNo}, #{title}, sysdate, 0, #{password}, #{content},
		0, 0)
	</insert>

	<!-- 게시글 읽기 -->
	<select id="read" resultType="board">
		SELECT board_no as boardNo, user_no as userNo, title,
		regdate, hit_count as hitCount, password, content,
		like_count as likeCount, hate_count as hateCount
		FROM 
		Board 
		where board_no = #{boardNo}
	</select>

	<!-- 게시물 삭제 -->
	<delete id="delete">
		delete from board where board_no = #{board_no}
	</delete>

	<select id="listAll" resultType="board">
 		SELECT board_no as boardNo, user_no
			as userNo, title, regdate, hit_count as hitCount, password, 
			content, like_count as likeCount, hate_count as hateCount
		FROM 
			Board
 	
	</select>

</mapper>